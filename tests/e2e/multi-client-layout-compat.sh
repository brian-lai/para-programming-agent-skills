#!/usr/bin/env bash
# E2E stub — phase 7 implements this.
# Validates that the skills directory layout is compatible across all target clients:
# Claude Code, OpenAI Codex, OpenCode, Cursor CLI, Gemini CLI.
echo "E2E stub — phase 7 implements this."
exit 1
